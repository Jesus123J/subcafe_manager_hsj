name: Maven to EXE

on:
  push:
    branches:
      - main  # Ejecuta el workflow en cada commit a la rama principal

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Clonar repositorio
      uses: actions/checkout@v4

    - name: Configurar JDK 22
      uses: actions/setup-java@v4
      with:
        java-version: '22'
        distribution: 'temurin'
        server-id: github
        settings-path: ${{ github.workspace }}

    - name: Compilar con Maven
      run: mvn -B package --file pom.xml

    - name: Verificar archivos en Launch4j
      run: |
        ls -R resources/launch4j/

    - name: Ejecutar Launch4j para convertir JAR a EXE
      run: |
        java -jar resources/launch4j/launch4j.jar resources/app_configuration_launch4j.xml

    - name: Renombrar ejecutable
      run: mv resources/Subcafae.exe "Subcafae - HSJv0.0.2.exe"

    - name: Subir ejecutable
      uses: actions/upload-artifact@v4
      with:
        name: Subcafae-HSJv0.0.2
        path: "Subcafae - HSJv0.0.2.exe"